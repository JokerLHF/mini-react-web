import { React, ReactDOM } from '../packages';

const Block = (
  <div>
    <p>I am</p>
    <p>hongfeng</p>
    true
    undefined
    null
  </div>
);

ReactDOM.render(Block, document.querySelector('#root'))
import { createElement } from './ReactElement';
import { useState, useEffect, useLayoutEffect, useCallback } from './ReactHook';

const React = {
  createElement,
  useState,
  useEffect,
  useLayoutEffect,
  useCallback,
}

export default React;